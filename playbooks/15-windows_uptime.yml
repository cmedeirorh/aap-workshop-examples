---
- name: Verify Windows Uptime
  hosts: windows
  
  tasks:
    - name: Install PendingReboot PowerShell module
      win_psmodule:
        name: PendingReboot
        
    - name: Execute command to test pending reboot
      win_shell: Test-PendingReboot -Detailed
      register: command
      changed_when: false
      check_mode: false
      
    - name: Print errors if any
      debug:
        var: command
