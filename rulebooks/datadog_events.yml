---
- name: Respond to Datadog Events
  hosts: all
  sources:
    - ansible.eda.webhook:
        host: 0.0.0.0
        port: 35001

  rules:
    - name: Datadog Alert - Web Monitoring
      condition: event.payload is defined 
      action:
        debug:
          var: event.payload.event_title
